{
  "private": true,
  "scripts": {
    "build": "elm-make src/Hexagon.elm --output=public/index.html",
    "server": "elm-reactor",
    "setup": "elm-package install && git submodule update --init"
  },
  "dependencies": {
    "elm": "^0.17.0"
  }
}
